# Memory Log - 2026-05-04

## PLCTools Task - Recording Bar Position Change (PLANNED)

**Status:** Plan stored, NOT YET EXECUTED

### Objective
Move the RecordingBar widget from above the QTabWidget to below it (bottom of window), making it a persistent status/action bar always visible regardless of active tab.

### File to Modify
- `C:\Users\thadd\Documents\PLCTools\src\plc_tools\gui\main_window.py`

### Change Required
**Current layout order:**
1. `layout.addWidget(connection_bar)`
2. `layout.addWidget(self._rec_bar)` ← above tabs
3. `layout.addWidget(tabs)`

**Target layout order:**
1. `layout.addWidget(connection_bar)`
2. `layout.addWidget(tabs)`
3. `layout.addWidget(self._rec_bar)` ← below tabs, at bottom

### Implementation Details
- Locate the block in `main_window.py` that sets up `_rec_bar` with signal connections
- Keep all setup code (signal connections, `set_connected`) in place
- Only move the `layout.addWidget(self._rec_bar)` line to after `layout.addWidget(tabs)`

### Post-Change Steps
1. Rebuild exe: `python build_exe.py`
2. Verify recording bar appears at bottom across all 6 tabs
3. Test Start/Stop recording functionality
4. Commit and push to GitHub

### Verification Checklist
- [ ] Recording bar appears at bottom of window
- [ ] Bar remains visible on all 6 tabs
- [ ] Start Recording opens file dialog
- [ ] LED turns red during recording
- [ ] Timer updates correctly
- [ ] Stop resets correctly

---

**Created:** 2026-05-04 00:41 CDT
**Execution pending:** Awaiting user confirmation to proceed

---

## Desktop Shortcut for PLC Coder - IN PROGRESS

**Time:** 2026-05-04 06:52 - 07:28 CDT

## Correct PLC Project Location (CRITICAL)

**Active Project:** Degater PLC Tool BST33 and 35
**Location:** `C:\Users\thadd\Documents\Degater PLC Tool BST33 and 35`
**NOT:** `C:\Users\thadd\Documents\PLCTools\` (old/deprecated location)

The active development is happening in the **BST33 and 35** folder, which has:
- PROJECT_MEMORY.md
- build_exe.py
- src/plc_tools/ source code
- dist/Degater PLCTool BST33 and 35.exe (built executable)

**Agent spawn command must use:**
`cwd="C:\Users\thadd\Documents\Degater PLC Tool BST33 and 35"`

---

## Desktop Shortcut for PLC Coder - IN PROGRESS

**Time:** 2026-05-04 06:52 - 07:28 CDT

**Goal:** Create a desktop shortcut that spawns the PLCTools Coder agent and opens the chat window.

**Files Created:**
- `scripts/launch-plc-coder.bat` - Main launcher (crashes at step 1/3)
- `scripts/launch-plc-coder-debug.bat` - Debug version to see error output
- `scripts/launch-plc-coder.ps1` - PowerShell version (unused)

**Problem:** 
- Shortcut crashes when executing `openclaw sessions spawn` from batch file
- Works fine when run manually in PowerShell
- Likely an environment/PATH issue when running from .lnk shortcut

**Current Status:** 
- Debug batch file created to capture error output
- Shortcut exists but needs troubleshooting
- **Workaround:** Tell Spock "spawn the PLC coder" to launch it manually

**Next Steps:**
1. Run debug batch file to see actual error
2. Fix based on error message, OR
3. Use alternative approach (PowerShell script with proper env loading)

---
