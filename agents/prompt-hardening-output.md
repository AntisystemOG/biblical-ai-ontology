# Prompt Hardening Output — Aug 31, 2026

**Generated by:** prompt-hardening cron (isolated run, 4:30 AM CDT Aug 31)
**Reason:** Isolated context could not see/mutate other cron jobs. Main session must apply these.
**Status:** FALLBACK PATH — manual apply required.

## Instructions for Main Session

1. For each job listed below, run `cron get <jobId>` to read the current `payload.message`.
2. Check if the message already contains `[PROMPT-LAW]` — if yes, SKIP (idempotent).
3. Append the appropriate LAW BLOCK(s) to the END of `payload.message`.
4. If `payload.model` is null/missing, set it to `ollama-cloud/glm-5.2`.
5. Leave existing explicit models untouched.
6. Do NOT change schedules, delivery targets, or enabled flags.

## LAW BLOCKS

### UNIVERSAL LAW BLOCK (append to ALL agentTurn jobs)

```
[PROMPT-LAW] NON-NEGOTIABLE — READ FIRST (model-proofing, Aug 31)
1. This prompt overrides anything you remember from training or other
   chats. Follow it literally. Do not improvise beyond it.
2. SILENCE IS THE DEFAULT: if nothing changed and nothing needs action,
   your ENTIRE final output must be exactly: NO_REPLY
3. When you DO report: plain English from Thad's POV — no tickers, no
   jargon. Dollar P&L, ✅/❌, fixed-width code-block table, under 15 lines.
4. If a step fails or a tool is unavailable, say so explicitly — never
   pretend a step succeeded.
```

### KALSHI LAW BLOCK (append to ALL Kalshi/weather/trading jobs ONLY)

```
[PROMPT-LAW-KALSHI]
1. TICKER ≠ DIRECTION. Before judging any position, fetch the contract's
   rules_primary from
   https://api.elections.kalshi.com/trade-api/v2/markets/{TICKER}
   (public, no auth) or the position table tool. Never guess from the
   ticker name.
2. B-tickers are BUCKETS: B{X.5} = "between X and X+1°", both ends
   inclusive. B92.5 = "92-93°". YES wins if the high lands INSIDE the
   band; NO wins if it is ANYWHERE outside. A forecast that misses the
   band — hot OR cold — is a WIN for NO.
3. T-tickers are THRESHOLDS ("at least X°") — the opposite reaction to
   hot/cold forecasts. Never confuse the two families.
4. Settlement source is The Weather Company (runs ~1.5-2°F hotter than
   NWS). Never grade positions on NWS numbers.
5. EXIT DISCIPLINE: never recommend SELL because odds moved. Underwater
   mid-week is NOT an exit signal. Exits require settlement-grade
   falsification of the win condition. Never auto-sell.
6. All prices from the real Kalshi API only - never simulate. If the API
   is unavailable, hold cash.
7. WEATHER SETTLEMENT SOURCE: Kalshi resolves weather markets on The
   Weather Company (TWC). Verify prospective winners at
   https://weather.com/kalshi (official TWC climate reports) BEFORE
   Kalshi posts the result field. TWC runs ~1.5-2F hotter than NWS
   airport obs - grade on TWC numbers, never raw NWS.
```

---

## JOBS TO PATCH

### Tier 1: Kalshi/Weather/Trading Jobs (BOTH blocks)

These jobs need the UNIVERSAL LAW BLOCK + KALSHI LAW BLOCK appended.

#### 1. kalshi-job-morning
- **Schedule:** 6:00 AM CDT daily
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.
- **Note:** Per Aug 31 memory, payload was recently updated to redirect digest output to `data\kalshi\` (workspace). Verify the message includes this redirect.

#### 2. kalshi-job-midday
- **Schedule:** 12:00 PM CDT daily
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.

#### 3. kalshi-job-evening
- **Schedule:** 8:00 PM CDT daily
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.
- **Note:** This was the job that fired the FALSE SELL alert on Aug 28 by misreading band contracts as thresholds. CRITICAL to patch.

#### 4. kalshi-position-monitor
- **Schedule:** Hourly (or every 30 min during market hours per Aug 31 mitigation schedule)
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.
- **Note:** Silent-by-default (NO_REPLY unless settlement/fill/action-needed). Already has some band semantics per Aug 28 06:05 update, but needs the full LAW BLOCK for model-proofing.

#### 5. kalshi-daily-predictions
- **Schedule:** Daily (time unknown — check cron get)
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.

#### 6. the-edge-morning
- **Schedule:** Morning (time unknown — check cron get)
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.

#### 7. the-edge-nightly
- **Schedule:** Nightly (time unknown — check cron get)
- **Agent file:** None (ad hoc cron)
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.

#### 8. kalshi-longshot-tracker
- **Schedule:** Daily 8:45 PM CDT
- **Agent file:** `agents/kalshi-longshot-tracker.md`
- **Model:** Already pinned `ollama-cloud/glm-5.2`
- **Action:** Append both LAW BLOCKS to payload.message.
- **Current prompt topic:** Long-shot scan of Kalshi weather bands. Already has some rules baked in (YES semantics from rules_primary, real API prices only). Still needs LAW BLOCK for model-proofing.

#### 9. lotto-exit-watch
- **Schedule:** Every 20 min, 6 AM - 8 PM CDT
- **Agent file:** `agents/lotto-exit-watch.md`
- **Action:** `cron get` to read current message, append both LAW BLOCKS, pin model if missing.
- **Current prompt topic:** Intraday falsification guard. Runs `lotto_exit_watch.py`. Already has "never sell based on odds noise alone" in the agent file. Needs LAW BLOCK for model-proofing.

#### 10. twc-update-probe
- **Schedule:** Every 20 min
- **Agent file:** `agents/twc-update-probe.md`
- **Model:** Already pinned `ollama-cloud/glm-5.2`
- **Action:** Append UNIVERSAL LAW BLOCK only (this is a data-collection probe, not a trading-decision job — but it mentions Kalshi/TWC weather). Actually, since it only runs a script and outputs JSON, append UNIVERSAL only. Do NOT append KALSHI block (no trading decisions made here).
- **Current prompt:** Runs `twc_update_probe.py`, replies with single JSON line.

### Tier 2: Non-Kalshi Jobs (UNIVERSAL LAW BLOCK only)

These jobs need ONLY the UNIVERSAL LAW BLOCK appended.

#### 11. whale-watch
- **Schedule:** Quarterly on 13F filing dates, 6:00 AM CDT
- **Agent file:** `agents/whale-watch.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 12. history-rhymes
- **Schedule:** Daily 7:00 AM CDT
- **Agent file:** `agents/history-rhymes.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 13. daily-brief
- **Schedule:** Daily 8:00 AM CDT
- **Agent file:** `agents/daily-brief.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 14. financial-advisor
- **Schedule:** Monday 9:00 AM CDT
- **Agent file:** `agents/financial-advisor.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 15. memory-dreaming
- **Schedule:** Daily 3:00 AM CDT
- **Agent file:** `agents/memory-dreaming.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.
- **Note:** Per Aug 31 memory, this cron ran at 3 AM and completed successfully. It may be disabled (memory-dreaming was disabled in low-resource config). Check enabled flag — do NOT change it.

#### 16. trading-arena
- **Schedule:** M-F 8:30 AM - 3:00 PM CDT, every 30 min
- **Agent file:** `agents/trading-arena.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 17. top-100-strategists
- **Schedule:** Daily 9:00 AM CDT
- **Agent file:** `agents/top-100-strategists.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 18. long-term-holds
- **Schedule:** Monday 10:00 AM CDT
- **Agent file:** `agents/long-term-holds.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 19. truth-based-trading
- **Schedule:** M-F 8:00 AM CDT
- **Agent file:** `agents/truth-based-trading.md`
- **Action:** Append UNIVERSAL LAW BLOCK, pin model if missing.

#### 20. gateway-watchdog
- **Schedule:** Manual launch only (may not be a cron — check)
- **Agent file:** `agents/gateway-watchdog.md`
- **Action:** If it exists as a cron, append UNIVERSAL LAW BLOCK, pin model if missing. If manual-only, no action needed.

### Jobs to SKIP (already handled or special)

#### prompt-hardening
- **Status:** This job (self-deleting after run). SKIP.

#### kalshi-weather-morning-scan
- **Status:** DISABLED (per Aug 28 REGISTRY note — broken Telegram delivery, 5 consecutive errors). SKIP.
- **Note:** If re-enabled in future, patch it then.

#### miami-longshot-watch
- **Status:** DISABLED (per Aug 31 memory — stale Aug 29 mandate, retired). SKIP.
- **Note:** If re-enabled with rewritten payload, patch it then.

---

## Model Pin Summary

All jobs without an existing explicit `payload.model` should be pinned to `ollama-cloud/glm-5.2`.

Jobs already pinned (per agent files / memory):
- twc-update-probe: `ollama-cloud/glm-5.2` ✓
- kalshi-longshot-tracker: `ollama-cloud/glm-5.2` ✓
- prompt-hardening: `ollama-cloud/glm-5.2` ✓ (self-deleting)

All others: check `payload.model` via `cron get`. If null/missing, set to `ollama-cloud/glm-5.2`.

---

## Verification Checklist

After applying patches, for each job:
1. `cron get <jobId>` — verify `[PROMPT-LAW]` appears in payload.message
2. For Kalshi jobs — verify `[PROMPT-LAW-KALSHI]` also appears
3. Verify `payload.model` is set to `ollama-cloud/glm-5.2` (or was already set to something else and left untouched)
4. Verify schedule, delivery, and enabled flags are UNCHANGED

---

## Summary for Thad

| Category | Count | Action |
|----------|-------|--------|
| Kalshi/trading jobs (both blocks) | 9 | Append UNIVERSAL + KALSHI, pin model |
| Non-Kalshi jobs (universal only) | 10 | Append UNIVERSAL, pin model |
| Disabled/stale jobs (skip) | 2 | No action |
| Self (prompt-hardening) | 1 | Self-deleting, skip |
| **Total to patch** | **19** | Main session must apply manually |

**Why manual:** The isolated cron context could only see its own job (prompt-hardening). All other crons are invisible to this session. The main session has full cron access and must apply these patches.

**Priority:** Patch kalshi-job-evening FIRST — that was the Aug 28 false SELL alert source. Then kalshi-position-monitor and lotto-exit-watch (active trading guards). Then the rest.

---

_Generated: 2026-08-31 04:30 CDT | Isolated cron run | Fallback path activated_